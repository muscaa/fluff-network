package fluff.network.packet;

/**
 * Marker interface for all packet types in the network communication system.
 * Classes implementing this interface represent packets that can be sent or received over the network.
 */
public interface IPacketBase {
	
}
